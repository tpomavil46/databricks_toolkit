"""
dlt — Delta Live Tables Patterns.

Modules
-------
expectations     Build DLT expectation configs (expect / expect_or_drop / expect_or_fail).
pipeline_config  DLT pipeline settings JSON builders.
table_sql        SQL templates for CREATE STREAMING TABLE and CREATE LIVE VIEW.
"""
